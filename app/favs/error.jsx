"use client"

// Unexpected runtime error in nested routing, show this page

// Granular error UI : favs childrens

const COM = () => {
  return <span> error page</span>;
};

export default COM;



{/* Loading optimization by next */}
        {/* Equivalent to creating loading.js, just Suspense more controllable */}
        {/* <Suspense fallback={<p>Loading more...</p>}>
        <div>Asynchronous, expensive rendering</div>
      </Suspense> */}