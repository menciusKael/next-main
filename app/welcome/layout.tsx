// Automatically generated, not required, if need Common Share

const COM = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      {/* Include shared UI here e.g. a header or sidebar */}
      <header>Common Share</header>
      {children}
    </section>
  );
};

export default COM;
