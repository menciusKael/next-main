const COM = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      {children}
    </section>
  );
};

export default COM;

// (folderName)
// The entire folder will be ignored by the routing system
